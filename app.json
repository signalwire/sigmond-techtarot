{
  "name": "techtarot",
  "description": "SignalWire TechTarot - Sigmond the AI tarot reader with tech-themed cards over WebRTC video",
  "repository": "https://github.com/signalwire-demos/techtarot",
  "keywords": ["signalwire", "ai", "tarot", "webrtc", "python", "video"],
  "env": {
    "SIGNALWIRE_SPACE_NAME": {
      "description": "SignalWire space name (e.g., 'myspace' or 'myspace.signalwire.com')",
      "required": true
    },
    "SIGNALWIRE_PROJECT_ID": {
      "description": "SignalWire project ID",
      "required": true
    },
    "SIGNALWIRE_TOKEN": {
      "description": "SignalWire API token",
      "required": true
    },
    "SWML_PROXY_URL_BASE": {
      "description": "Public URL base for SWML callbacks (e.g., 'https://techtarot.signalwire.me')",
      "required": false
    },
    "SWML_BASIC_AUTH_USER": {
      "description": "Basic auth username for SWML endpoints (optional, auto-generated if not set)",
      "required": false
    },
    "SWML_BASIC_AUTH_PASSWORD": {
      "description": "Basic auth password for SWML endpoints (optional, auto-generated if not set)",
      "required": false
    },
    "AGENT_NAME": {
      "description": "Name for the SWML handler resource",
      "value": "techtarot"
    },
    "PORT": {
      "description": "Server port",
      "value": "5000"
    },
    "POST_PROMPT_URL": {
      "description": "Optional URL for post-prompt webhook to receive conversation summaries",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  "healthchecks": {
    "web": [
      {
        "type": "startup",
        "name": "port listening",
        "listening": true,
        "attempts": 10,
        "wait": 5,
        "timeout": 60
      }
    ]
  }
}
